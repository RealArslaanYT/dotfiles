alt - j : yabai -m window --focus west
alt - k : yabai -m window --focus south
alt - i : yabai -m window --focus north
alt - l : yabai -m window --focus east

alt - s : yabai -m display --focus west
alt - g : yabai -m display --focus east

shift + alt - r : yabai -m space --rotate 270
shift + alt - y : yabai -m space --mirror y-axis
shift + alt - x : yabai -m space --mirror x-axis
shift + alt - t : yabai -m window --toggle float --grid 4:4:1:1:2:2

shift + alt - m : yabai -m window --toggle zoom-fullscreen
shift + alt - e : yabai -m space --balance

shift + alt - j : yabai -m window --swap west
shift + alt - k : yabai -m window --swap south
shift + alt - i : yabai -m window --swap north
shift + alt - l : yabai -m window --swap east

ctrl + alt - j : yabai -m window --warp west
ctrl + alt - k : yabai -m window --warp south
ctrl + alt - i : yabai -m window --warp north
ctrl + alt - l : yabai -m window --warp east

shift + alt - 1 : yabai -m window --space 1;
shift + alt - 2 : yabai -m window --space 2;
shift + alt - 3 : yabai -m window --space 3;
shift + alt - 4 : yabai -m window --space 4;
shift + alt - 5 : yabai -m window --space 5;
shift + alt - 6 : yabai -m window --space 6;
shift + alt - 7 : yabai -m window --space 7;

# keybinding to fix occasional yabai glitch
ctrl + shift - l : yabai -m space --layout bsp

ctrl + shift - j : osascript -e "set volume output volume ((output volume of (get volume settings)) - 2)"
ctrl + shift - k : osascript -e "set volume output volume ((output volume of (get volume settings)) + 2)"

ctrl + shift - i : brightness $(echo "$(brightness -l | grep brightness | awk '{print $4}') - 0.01" | bc)
ctrl + shift - o : brightness $(echo "$(brightness -l | grep brightness | awk '{print $4}') + 0.01" | bc)

cmd - return : open -n -a iTerm
cmd - b : open -n -a Firefox
cmd - e : osascript $HOME/.NewFinderWindow.scpt

cmd + alt - r : yabai --restart-service && brew services restart sketchybar
